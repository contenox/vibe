// Package taskengine runs the steps inside a chain. Each step can call an LLM, invoke a hook, execute a shell command, or branch on output. Chains are defined in JSON you version in git.
package taskengine
