# Chat

Use `@contenox` in VS Code Chat for workspace questions, code explanations, and
repeatable local workflows.

Contenox keeps chat sessions in local runtime state. The first prompt in a new
session becomes the session title so the Sessions view stays readable.

Try a prompt such as:

```text
Summarize this workspace and call out the main runtime entry points.
```
