# Sessions

The Contenox activity bar view lists local chat sessions for the current
workspace.

Use it to reopen a previous session, refresh the list, or delete sessions that
are no longer useful.

Session titles are derived from the first meaningful prompt or slash command,
instead of generic names like `default`.
