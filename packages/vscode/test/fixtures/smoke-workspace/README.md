# Contenox VS Code Smoke Workspace

This fixture opens in the Extension Development Host for VS Code integration
tests. It disables bridge startup so contribution smoke tests do not require a
configured model provider.
