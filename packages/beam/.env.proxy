# Used with: npm run dev:proxy  (vite --mode proxy)
# 1) Start API + static host: contenox beam
# 2) From packages/beam: npm run dev:proxy — Vite proxies /api → backend, same-origin cookies.
VITE_DEV_API_PROXY=1
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8081
