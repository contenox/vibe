# Work tracker

Maintainer/agent working state. One line per item. This folder is unpublished
but the repo is public — no credentials, account identifiers, or
reproduction-grade security detail in here.

## V1 TODAY — critical path (2026-07-28, in order)

1. [x] VERIFICATION GREEN (2026-07-28 evening): full `-short` suite passes serially (`-p 1`), zero failures; build/gofmt/vet clean; dangling refs swept; comment tally 29,279→16,583 prod / 11,677→5,806 test. Approval-hang root cause fixed (late verdict had no path back — now forwards through Respond and resumes the checkpoint; failing-test-first, 5x race-clean); find_files allow-tier everywhere + read-only drift test; approval lifecycle logs to beam.log THROUGH the redacting tracker (the slog guard test caught the agent's direct-slog shortcut; rewired via chatOpts.EffectiveTracker → BuildEngine, bridge Deps.Tracker, beam's NewTextActivityTracker over beam.log — fleet + beamHITL got the real tracker too); diff annotations for whitespace-only and duplicate-line edits; recall-on-cancel + composer history; grace-reap noted as pre-existing timing flake (5/5 green isolated)
2. [x] Beam tailoring slice SHIPPED: hitl-policy-beam.json (acp copy + attended-session header) + chain-beam.json (full coding doctrine prompt) seeded, beam_cmd defaults to both (CONTENOX_BEAM_CHAIN_PATH override); DISCOVERY: beam always had the code-intel toolsets via BuildEngine — the gap was the EDITOR path (runACPProfile hand-rolled 5 tools); acp_toolset.go now gives Zed/JetBrains gointel/workspace/git/jq/goja too; all tests green
3. [ ] Dogfood pass (maintainer): one coding task + one ops task in beam on this repo; fix-or-accept findings. HEADLESS SMOKE PASSED (2026-07-28): fresh scratch module, planted one-line bug + failing test → `contenox --auto --shell` on wired vertex/gemini found it, made the exact minimal one-line fix, ran go test, reported accurately — independently verified. Default chain, not even the beam doctrine chain. Watch-items in the new prompt: REFUSE-UNCLEAR may over-ask on simple tasks; write_file's never-overwrite rule fights legitimate wholesale regeneration
4. [x] Docs catch-up DONE (website build green, anchors verified): edit_file/grep-recursion/`**` documented incl. approval-diff section; env-scrub page rewritten to present truth; beam preset/chain + CONTENOX_BEAM_CHAIN_PATH documented (+ corrected the false shared-env-var claim); five toolsets marked available in beam AND acp sessions; serve ghost purged from acp_cmd/workspace_cmd/mission_supervision with subscriber-verified wording; 7 more stale pages caught by grep and fixed
5. [ ] Release gate — ONLY MAINTAINER STEPS REMAIN:
   - history rewrite of OUR OWN repo (contenox/beam) via force-push — the leak purge, same operation as the 2026-07-26 purge; affects no other project. Name COMMITTED PERMANENT (2026-07-28): the ACP registry requires a stable repo name, so the rename question is closed; module path permanent too. Sequencing only: finish our own history purge before updating registry PR #353 (which is updated the NORMAL way — ordinary commits to our fork's PR branch — never any force-push to the registry repo)
   - regression greps: `blueprints/|TODO.md|pando|modeld|contenox serve|CONTENOX_SERVER_URL`
   - `acp_cmd.go` help: drop the `CONTENOX_SERVER_URL`/serve claim (last advertised-but-absent item)
   - tag + release build + install.sh sanity against the new assets
6. [x] R&D section SHIPPED (revised 2026-07-28 evening: the three repacked research essays were PULLED from the public lab and moved to docs/development/internal/tone-samples/ as house-voice reference — the lab stays product-lines-only) (2026-07-28 evening): docs/rnd/{index,beam-web,modeld,vscode-extension,api-layer,ui-library}.md recovered from pre-purge git sources — wins-tone, indexed, 11 legacy URLs + 2 dead astro redirects retargeted to R&D pages, video/poster S3 rewriting extended in remark-md-links, build green (58 pages), leak grep clean (two archive screenshots excluded for leaking a username). MAINTAINER STEP: upload the 7 assets per docs/development/internal/rnd-assets.md to the media bucket
7. [x] Branding pass SHIPPED: hero already compliant; definitional lead rewritten to imperative; envelope card retitled to the walk-away proof; trust chips added EN+DE; homepage + 13 pages gained outcome-first meta descriptions; THEME: beam-gold token ramp was defined but unused (site rode Tailwind amber + a pre-rebrand indigo glow) — swept onto primary-* tokens both schemes, ramp completed (950), theme-color meta aligned; build green (58 pages). Playwright MCP unreachable from agents — no screenshots, reported honestly; preview manually. README "beam — soon" fixed to shipped + OpenClaw added. Follow-up decision parked: surface-* token family unused outside body — deprecate or migrate
8. [x] EE product lines → R&D SHIPPED: docs/rnd/{bob,runner,vald-operator,blueprints}.md + index cards — capability-level, zero leak hits, continuity lines verified against real current features; pilot/cloud/pricing correctly declined as SKU/marketing labels; no usable archive media found (no manifest additions); build green, 62 pages
9. [x] BRAND: MINT — COMPLETE (2026-07-28 evening): emerald/mint ramp, core #34d399, chosen by eye + the terminal-native argument. Applied everywhere: website tokens + hero glow (render "break" was a phantom — user's browser was on a dead localhost port; e2e agent proved render with screenshots), wide logos + full favicon set (role-mapped tri-color mint), beam TUI style table (truecolor/256/ANSI-16 with verified cube math; ANSI-16 upgraded from bold-only to real green; zero goldens churned — testkit's symbolic style tags made the recolor invisible to fixtures; suite green). Section renamed "Lab". Rule stands: future color changes are decision-only, one ramp edit
10. [ ] ACP registry: PARKED (PR #353 closed by maintainer-of-this-repo 2026-07-28; empirical check of the registry's last 100 PRs: zero new independent agents merged — only incumbent version bumps and bots; all 33 open PRs are hanging independent submissions, pando included since June). V1 does NOT gate on it. Distribution channels that need no approval: documented Zed agent_servers config, JetBrains setup, install.sh. Evaluate the Zed extensions marketplace as the active community channel. Resubmit to the registry only if its intake visibly unfreezes.
7. [ ] Resurrect agent.json + icon.svg into the repo from git history (canonical copy lives here, ready for any future submission)
8. [ ] Repo/module name: no deadline anymore (registry pressure gone); settle before the v1 tag for module-path stability — pure branding call: contenox/beam vs contenox/runtime vs contenox/contenox

## The ladder (2026-07-28): what happens if traffic hits — every rung has its decided mechanism

1. SURVIVE THE HIT: hard spend cap + kill switch (no bankruptcy-by-virality); purpose-gate filters tourist load; doctor/vet/teaching-errors deflect the support flood; behavior suite + drift tests do the triage (the anti-Aider-backlog design)
2. EARN: gigs lane NOW (the lab is the sales asset — workflow-setup/sprint engagements) + funnel lane ramping (gateway → metered tier → Stripe; BYOK/self-host as the trust-conversion argument)
3. RAISE (optional): with real assets — research lineage + reach, the durability moat, the runtime thesis, funnel conversion data; purpose of the raise: pager off the founder, then a client dev for new-beam
4. THE FLOOR: contenox idles without dying — sheddable costs, no funding clock; waiting is always affordable. The missing input on every rung is the same one: first traffic → which is the V1 tag + un-gated posts

## Positioning (2026-07-28): bottom-up only, end-user language only

- Artifact register (dogfood find): contenox's answers are impersonal, self-contained DOCUMENTS — paste-ready for Slack/wiki/tickets without edits — unlike conversational assistants whose answers address the user and need rework before sharing. Now codified in the beam chain (ARTIFACT REGISTER rule; endings scoped to work turns, one detachable line). This is a real differentiator for the docs/tickets/PM flows: every turn produces a deliverable. Use in copy + playbooks.

- B2B / mandated-by-techlead route is OUT (no resources for the sales motion). Bottom-up requires felt-pain vocabulary: the envelope sells as "an agent you can walk away from" (fixes approval fatigue AND yolo-fear) — the word "govern"/"governance" is banned from user-facing copy; compliance framing only ever as a secondary line for readers who already think that way
- Traction lesson (the React UI was the only thing that ever pulled): people adopt what they can SEE — visible artifacts are first-class deliverables (recordings, screenshots); parked candidate at solo budget: a shareable read-only run page (one mission: transcript, the ask, the resume) — one page, not an IDE
- Purpose-gated by design (2026-07-28): contenox requires intent to use — it ships work, and calls out aimlessness (REFUSE UNCLEAR). This forecloses hype (toys are usable without purpose) and selects the users worth having. Consequence: marketing must SUPPLY the purpose — every playbook/demo leads with a borrowed outcome, never with the tool
- Platform-dependence is never load-bearing again (Ollama→cloud, VS Code→gated, registry→frozen taught the same lesson): every ecosystem is optional reach; the binary + un-gated channels are the foundation

## Distribution (post-tag; only un-gated channels — decided 2026-07-28)

- [ ] Homebrew tap, AUR package, nixpkgs submission (community channels that merge independents; audience = self-hosters/ops, the wedge)
- [ ] The three-minute proof recording: mission fired → envelope catches a destructive call → approval answered from another terminal → exactly-once resume; all on Ollama (no hosted keys, no reselling exposure)
- [ ] Post where the un-gated audience lives (HN Show, r/selfhosted, lobste.rs) with the ops story, not the coding-agent story
- [ ] Platform registries (ACP/MCP lists, marketplaces): protocol compliance yes, dependence no — resubmit only when intake visibly moves
- [ ] Dormant VS Code marketplace listing: sunset with a pointer to the ACP setup, or leave parked — never resume the dual extension+ACP maintenance tax
- [ ] Proof-story playbooks (post-tag, one per week; each = runnable chain in examples/ + recording + writeup with real numbers, AND doubles as a behavior-suite fixture): candidates — leads→rated Notion notes with ready-to-fire template mails; $-saved-via-caching (numbers from our own cache-aware usage extraction); authorized-CTF (HTB) run under the envelope; "shipped X via these prompts". Every playbook ships two reproduction paths — self-serve free Gemini key (recommended: real tool-calling quality, $0, ToS-clean, with the honest free-tier-data footnote) and vLLM for private code — we never distribute, funnel, or host keys/models ourselves
- [ ] Model strategy (2026-07-28, corrected): Ollama demoted to embeddings/index duty in docs; Gemini free tier = recommended onboarding provider at launch; vLLM = the private path; recordings/evals on spot GPUs by the hour. CORRECTION: 2026 small models (Devstral-Small-class 8–24B) hold agentic loops, and the harness LOWERS the model floor (typed tools + teaching errors + narrow verb vocabulary) — a measurable claim the behavior suite should certify per model
- [ ] BUSINESS MODEL DECISION (2026-07-28, supersedes "community tier as growth experiment"): the free-hosted-tier-with-upgrade-funnel is the universal commercial pattern (zero exceptions among businesses; Aider the non-business exception proving it) and we enter it AWARE: this is inference retail — metering, tokens, abuse defense, Stripe, GDPR/AVV + retention policy (DE jurisdiction), uptime obligations ("the walk-away product acquires a pager"), and a permanent focus tax (billing/support/abuse hours). Differentiator: BYOK/self-host stays first-class — "upgrade for convenience, leave for free" is the conversion argument for our audience, not a leak. Free tier = tight cap + funnel mouth, never a lifestyle; hard monthly spend cap + kill switch = bootstrapper survival clause. First concrete tasks (post-V1): ToS/privacy/AVV draft, Stripe + token issuance, then the gateway below. PRICING CALIBRATION (2026-07-28, market-verified): floor $10 (Copilot), modal $20 (Cursor/Windsurf/Claude Pro — Windsurf RAISED 15→20 May 2026), real full-time cost ~$100 (Claude Max 5x / Cursor Pro+ $60), heavy $200, teams $19-40/seat, credits-with-caps universal + credit-opacity is a market-wide sore point. OUR SKETCH: Free (tight budget) → Solo €12-15 (undercuts modal; margin holds on Flash-class COGS) → Ops ~€40 evidence-gated; THE FRAME: a plan is literally an ENVELOPE — a transparent, inspectable monthly spend budget in the product's own vocabulary (anti-credit-opacity differentiator); pricing page openly states heavy users should self-host (trust posture)
- [~] EE-as-hosting-foundation (survey agent running, 2026-07-28): the private monorepo holds the phase-2 infrastructure — multi-tenant SaaS boilerplate, k8s/helm engine, the site deploy pipeline, and possibly modeld as a future contenox-OWNED model provider (would replace the rented-vLLM phase-2 and complete the sovereignty story). Plan: launch + screenshot the old UIs (fills the Lab's missing captures), then keep/wipe map → strip to the SaaS+platform core. No deletions without explicit maintainer go
- [ ] Community free tier gateway (the infrastructure for the above): HOST THE GATEWAY, NOT THE MODEL. Phase 1: thin gateway (token auth + per-install rate limits + hard monthly cap + kill switch) proxying paid Gemini Flash — instant, quality-certain, usage-shaped cost, no GPU ops; check whether free-credit ToS permit backing the service, else paid Flash is tens of $/mo at modest traffic. The endpoint URL is OURS (client speaks custom-URL+token already, zero changes) → Phase 2 swaps the backend to a self-hosted vLLM box only after traffic proves demand AND the behavior suite certifies an open model — no user notices the swap. Honest docs line: community model is hosted-provider-backed; private work brings its own key/GPU. Serverless rejected (cold starts kill the first turn)

## Punch list (not blocking today)

- [ ] Favicon tab-contrast (deferred 2026-07-28 after experiments): production originals restored byte-for-byte (all 6 assets pulled from live). Tried and parked: solid dark, solid #7c3aed, scheme-split ramp, white sticker halo, violet scale-halo (no inner rim), stroked halo (inner rim, but not right). Candidates + composites live in the session scratchpad brand/ dir; revisit with fresh eyes

- [ ] Orphaned code wire-or-delete: terminalservice+terminalstore, contextasm, accessview, agentview, providerservice (~3.4k LOC); missionchanges (wire a `mission changes` verb or delete)
- [ ] Cancelled-turn "unsend" (investigated, declined as non-small): the cancelled user message persists unconditionally (agentservice/agent.go:223 via WithoutCancel); naive skip-guard at the poison-pill seam (agent.go:211) would also discard streamed partial content — needs real-assistant-text inspection vs failure-annotation-only. Seam + trap documented in the recall agent's report
- [ ] NEW (docs-agent find, 2026-07-28): workspace grants are UNWIRED — RootsChangedSubject has zero subscribers, vfs.NewFactory has zero callers, acp/beam pass WorkspaceRoots nil; the `workspace` verbs write config nothing reads. Wire grants into session composition or delete the verb family. Help text already reworded to the stored-not-enforced truth. Related: SANDBOX_TERMINAL_SCRUB's only consumer is the orphaned terminalservice
- [ ] AGENTS.md injection absent from ACP sessions, beam, and mission units
- [ ] Second harsher compaction pass if the literal 20% comment target is still wanted (fleet converged at 40–60% = contract floor)
- [ ] Noise-filter matcher triplication (localtools, workspaceindex, beamtui/fileaddr)
- [ ] Website npm tree: astro major upgrade (audit debt)
- [ ] Decommission the retired modeld artifacts bucket
- [ ] Old module path retract directives + pkg.go.dev removal request for the stale runtime page

## Surface portfolio (2026-07-28): purpose per surface, revival gates

A surface = a thin renderer over the session contract (chain + envelope + toolset + session semantics); ACP is one transport at the edge, never load-bearing. Live: CLI (control plane: config/envelopes/missions/approvals/headless), beam (everywhere-console: attended sessions anywhere the binary lands), editors-via-ACP (coding chair: client brings the editing machinery). Revival candidates, in order: (1) React web — the VISIBLE surface, two jobs only: shareable read-only run page + operator board (browser approvals = the on-call/phone story); explicitly NOT an IDE. (2) VS Code extension — connector-only ACP shim using the existing listing; last because the platform is hostile and the dual tax is real. Gates for any revival: one purpose no live surface serves + zero runtime changes + an evidence trigger from real usage; one at a time, none before V1.

## Decisions parked (maintainer)

- [ ] toolguidance: delete like the filter engine, or keep and declare in the envelope (A/B never ran)
- [ ] Behavior suite: headless ops-shaped fixtures scored from the event journal (completion, asks, denials, tool-abandonment); becomes the release gate after V1
- [ ] Envelope tooling: policy advisor (ask-history → proposed rule diffs, approve-only path) and OpenAPI slimmer (spec → subset + inject_params + suggested rules)
- [ ] Mission re-entry (may a finished mission wake its supervisor)
- [ ] CLAUDE.md: codify the session rules (policy is data; no hidden layers; delete over tune; comments are public API docs; nothing advertised that isn't enforced)
- [ ] (superseded, for the record) beam extraction to its own non-Go repo as a standalone ACP client — returns only if tailored-config beam still can't code; the frozen-ops-console + protocol-first-extensions analysis from 2026-07-28 stands if reopened

## Shipped 2026-07-28

- Envelope truth: hardcoded fallback policy + safeShellPrefixes deleted — fallback is rule-free fail-closed; secret quarantine + sensitive-file ask tier moved into hitl-policy-default.json; git shell prefixes removed everywhere (git subprocess executes repo config); `.git/**` write wall in default/acp/strict; invariants pinned by tests
- Output-filter feature deleted entirely; spool/truncation intact
- `edit_file` shipped (exact-string replace, envelope deny-triples, drift test verified-to-fail); recursive dir grep; `**` globs in find_files; gointel Invalidate wired to all fs mutations (spy-tested)
- Env-scrub rewired: one composition at all three spawn roots; `sandbox env` preview = applied; planted-secret tests per path
- Leak remediation: blueprints tree (incl. both mining reports) + TODO.md deleted; fixtures genericized; install.sh modeld branch gone; phantom `--setup-web` + dead browser auth handler gone; pando/ledger codes swept
- Comment compaction, full fleet (~29.3k → ~17.6k prod-file comment lines measured pre-tally): models 2305→1614 · gointel 964→296 · gojatool 1148→581 · A–G total 5677→2089 · H–M 6078→3631 · N–Z 3117→1504 · kernel 3640→2572 · acpsvc+fleetboot 4622→2211 · contenoxcli 3045→1699 (+ policy JSON 22.7KB→6.5KB) · beamtui 7579→4341 · libs/store/cmd/libacp 5210→3083
- Docs rewritten to public conventions (3 scopes, website build green): CLI reference gained mission/approvals/inbox/vet/index/beam families, lost phantom verbs; hitl.md corrected to fail-closed; wrong defaults fixed; five CLI-only toolsets documented and flagged
- Dead browser auth method removed; legacy website redirects retargeted; dev-docs links fixed
