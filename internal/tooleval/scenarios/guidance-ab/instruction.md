This workspace has several services under `services/`, plus supporting code under
`internal/` and notes under `docs/`. Many files share the same names across
directories (`handler.go`, `config.go`, `README.md`, `util.go`).

Task: determine the request timeout used by the **billing** service, and report both
the timeout value and the exact file path where it is defined.

Explore the tree with the filesystem tools to find it. When you have the answer, state
the value and the path in a final message.
