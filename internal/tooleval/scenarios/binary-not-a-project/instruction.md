Find the `widget` project in this workspace and append the following line, exactly, as
the final line of its README file:

    Reviewed by tool-eval harness.

Notes:

- The project's source lives somewhere under this workspace. Use the filesystem tools
  to locate it before editing anything.
- Append the line to the project's existing README (keep the current contents); do not
  create a new README elsewhere and do not overwrite unrelated files.
